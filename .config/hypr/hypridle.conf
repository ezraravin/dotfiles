general {
    lock_cmd = hyprlock
    before_sleep_cmd = hyprlock
}

listener {
    timeout = 300  # 5 min inactivity lock
    on-timeout = hyprlock
}

listener {
    event = "lid closed"
    on-event = hyprlock
}
